<%# Inventory Classification Addition View %>
<div class="form-container">
    <h1>Add New Classification</h1>

    <% if (message && message.length> 0) { %>
        <div class="notice">
            <%= message[0] %>
        </div>
        <% } %>

            <% if (errors) { %>
                <ul class="notice">
                    <% errors.array().forEach(error=> { %>
                        <li>
                            <%= error.msg %>
                        </li>
                        <% }) %>
                </ul>
                <% } %>

                    <p>Classification name cannot contain spaces or special characters.</p>

                    <form action="/inv/add-classification" method="post">
                        <label for="classification_name">Classification Name</label>
                        <input type="text" id="classification_name" name="classification_name" required minlength="3"
                            pattern="^[A-Za-z0-9]+$" placeholder="Letters and numbers only (no spaces)" value="<%= classification_name %>">
                        <button type="submit">Add Classification</button>
                    </form>
</div>